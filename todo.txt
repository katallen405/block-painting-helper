 ros2 launch nav2_bringup navigation_launch.py   use_sim_time:=false   params_file:=/home/katallen/sandbox/src/block-painting-helper/hayley-nav2/nav2_params.yaml

ros2 run nav_to_goal turtlebot_bridge 

ros2 launch slam_toolbox online_async_launch.py use_sim_time:=False



- Actually launch moveit in launch file
(stretch)- get UI have pictures of blocks which send colors to color_picker_node via statemachine
(stretch)- get UI to make webpage to show which block is wanted?

- measure turtlebot height
- measure turtlebot and supply closet positions

Cleanup after demo:


DONE
- add launching camera to demo.launch.py (nope, has to be done on local machine)
0 turtlebridge
1. nav_to_goal/bringup  — depth->scan, slam_toolbox, Nav2, navigator_node
  2. person_tracker       — camera, static TF, person tracker, top-down viz
  3. simple_sm_node       — top-level SMACH state machine
  4. bph_pickmeup_node    — arm pick-and-place action server
  
- bph_statemachine can swap between position controller and effort controller when appropriate
- calibrate camera
ros2 run camera_calibration cameracalibrator --size 7x9 --square 0.02 --ros-args -r image:=/bph_overhead_camera/image_raw
